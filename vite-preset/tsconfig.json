{
    "extends": "@greendrake/dev-config/tsconfig/node.json",
    "compilerOptions": {
        // node.json pins "types" to node only; the unit tests need bun:test.
        "types": ["node", "bun"]
    },
    "include": ["src", "test"]
}
